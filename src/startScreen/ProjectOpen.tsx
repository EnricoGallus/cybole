import {Button} from "@geist-ui/core";

function ProjectOpen() {

    return (
        <Button onClick={() => {

        }}>
            Open Project
        </Button>
    )
}

export default ProjectOpen;